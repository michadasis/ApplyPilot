# Auto-Apply Job Bot — Python dependencies
# Install with: pip install -r requirements.txt
# Then run: playwright install chromium

# Core automation
playwright==1.44.0              # Browser automation (Chromium)

# AI / LLM
google-generativeai>=0.8.0     # Google Gemini API client (free tier available)
                                # Get a key at: https://aistudio.google.com/app/apikey

# Resume parsing
pdfplumber==0.11.0              # PDF text extraction (handles multi-column)

# Configuration
python-dotenv==1.0.1            # .env file loader

# HTTP (used by the RemoteOK scraper; also a transitive dep of anthropic)
httpx>=0.27.0
